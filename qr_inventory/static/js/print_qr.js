// Автоматически запускаем печать при загрузке страницы
window.addEventListener('load', function() {
    // Небольшая задержка для загрузки всех изображений
    setTimeout(function() {
        // Раскомментируйте строку ниже, чтобы автоматически открывать диалог печати
        // window.print();
    }, 500);
}); 